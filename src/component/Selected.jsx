import React from 'react';
import PropTypes from 'prop-types';

const Selected = ({takaData}) => {

    return (
        <div>
            <h1>Hello Selected {takaData.length}</h1>
            
        </div>
    );
};

Selected.propTypes = {
    
};

export default Selected;